// packages/renderer/src/pages/SyncReportsPage.tsx
import { Title } from '@mantine/core';

export function SyncReportsPage() {
  return (
    <div>
      <Title order={2}>Sync Reports / History</Title>
      <p>List of past sync runs and details (including conflicts/orphans) will go here.</p>
    </div>
  );
}